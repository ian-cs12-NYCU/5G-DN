# Mosquitto 基本設定檔

# 監聽設定
listener 1883 0.0.0.0
protocol mqtt

# 允許匿名連線（開發用，生產環境建議改為 false 並設定帳號密碼）
allow_anonymous true

# 日誌設定
log_dest stdout
log_type all

# 持久化設定
persistence true
persistence_location /mosquitto/data/

# 自動保存訊息的間隔（秒）
autosave_interval 60
